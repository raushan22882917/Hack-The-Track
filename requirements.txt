# Telemetry Rush - Unified Requirements
# Install all dependencies: pip install -r requirements.txt
# All backend services are consolidated in fastapi-server/main.py

# FastAPI Server Dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data Processing Dependencies
pandas>=2.0.0
python-dateutil>=2.8.2
numpy>=1.24.0

# Note: 
# - asyncio is built-in, no install needed
# - websockets is optional (only for WebSocket endpoints, SSE is recommended)
# - All services run in one file: fastapi-server/main.py

